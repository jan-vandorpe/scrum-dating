

<!DOCTYPE html>
{{ include('head.twig') }}

    <body>
        {{ include('header.twig') }}
        
        {{ include('registreer.twig') }}

        {{ include('footer.twig') }}
       
    </body>

</html>